<% content_for :profile do %>
<%= render partial: 'profile', locals: {user: @user} %>
<% end %>

<% content_for :content do %>

<div class="follower list-group">
  <% @followers.each do |user| %>
  <%= render partial: 'user', locals: {user: user} %>
  <% end %>
</div>

<% end %>
